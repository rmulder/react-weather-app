export type CountryType = {
  isoCode: string;
  latitude: string;
  longitude: string;
  name: string;
};

export type CityType = {
  placeId: string;
  description: string;
  formatted_address: string;
};
